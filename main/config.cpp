#include "config.h"


void setupDevice() {
  /*
  get code from http server but for now
  */
  int otc = 123456;
  registerMessage(otc);
  // code displayed to user, now we would wait for data from server


}